# Field Aliases (Auto-maintained by self-annealing)
# Format: "ClickUp field name as found" → "Canonical name used in directive"
# When fuzzy matching resolves a mismatch, an entry is added here automatically.

## Active Aliases
(none yet — populated on first mismatch resolution)

## How to read this file
Each entry means: "When the ClickUp workspace has a field named X,
treat it as canonical field Y from the directive mapping table."

Example (after a future anneal event):
- "Spend" → "Amount spent (USD)"
- "ROAS" → "Purchase ROAS (return on ad spend)"
